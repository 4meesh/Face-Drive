{
  "name": "facedrive",
  "version": "1.0.0",
  "description": "Face scanning application for Google Drive",
  "scripts": {
    "start": "cd frontend && npm start",
    "backend": "cd backend && python app.py",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && pip install -r backend/requirements.txt",
    "dev": "concurrently \"npm run backend\" \"npm run start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@react-oauth/google": "^0.12.1"
  }
}
